/**
 * UpgradeScene.js — stub (replaced by in-menu upgrades system)
 * All meta-progression is now handled in MenuScene.
 */
class UpgradeScene extends Phaser.Scene {
    constructor() { super({ key: 'UpgradeScene' }); }
    create() {}
}
